import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.christianroberthchaponan.telenorappdev',
  appName: 'telenorApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
